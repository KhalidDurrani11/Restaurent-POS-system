# Netlify build for Khan Medical POS (Vite + React)
[build]
  command = "npm run build"
  publish = "dist"

# SPA: all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
